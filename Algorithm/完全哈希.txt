【Perfect Hash】（完全哈希）
完全哈希并无法保证百分之百的不碰撞发生，但是其碰撞发生的概率不到一半。
在perfect hash中，使用二级哈希的结构：

第一级存储 二级表的大小、二级表的Hash函数随机因子、指向二级表的指针
第二级hash表才存储数据，而且只存储 表大小的 1 / 2 次幂

每级表都使用Universal Hash。
（完全哈希的证明过程中使用到了Markov不等式的定理）